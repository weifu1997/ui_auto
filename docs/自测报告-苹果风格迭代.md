# 自测报告：苹果风格深化迭代 + UI 展示问题修复

- 自测日期：2026-08-13
- 自测范围：本轮全部改动（苹果蓝主色 / 浅色毛玻璃侧栏与顶栏 / 密度与排版 / 卡片与动效 token / 移动端抽屉导航 / 表格按钮统一 / 深色图标底纹）
- 执行方式：自动化门禁按 `test:all` 顺序分步执行 + Playwright 真实浏览器全流程旅程（截图存档于 `docs/自测截图/苹果风格迭代/`）

## 1. 自动化门禁（9/9 通过）

| # | 门禁 | 结果 |
| --- | --- | --- |
| 1 | `npm run build`（TS + Vite 生产构建） | ✅ 通过 |
| 2 | `npm run lint`（oxlint） | ✅ 0 错误 0 警告 |
| 3 | `npm run test:unit`（Vitest） | ✅ 8 文件 54 用例全部通过 |
| 4 | `npm run test:platform`（平台契约） | ✅ Platform contract smoke test passed |
| 5 | `npm run test:managed`（受管执行器） | ✅ ManagedRunner smoke test passed |
| 6 | `npm run test:worker`（Worker 冒烟 ×5） | ✅ worker / persistence / launch-failure / secret / local-picker 全部 passed |
| 7 | `npm run test:e2e`（Playwright 全量） | ✅ 25/25 通过（含完整用户旅程、平台同步、密钥注入、真实 Worker 验证） |
| 8 | `npm run test:production`（生产模式 UI） | ✅ Production UI smoke test passed |
| 9 | `npm run test:windows`（部署脚本） | ✅ Windows deployment script smoke test passed |

## 2. 用户视角全流程旅程（Playwright 实测）

前置：全新本地工作空间（local 模式）。截图见 `docs/自测截图/苹果风格迭代/`。

| 步骤 | 操作 | 结果 |
| --- | --- | --- |
| 1 | 新建「全流程自测项目」 | 创建成功（`01-新建项目.png`） |
| 2 | 环境页新建「自测环境」（baseUrl=https://example.test） | 环境卡片出现，顶栏环境下拉同步（`02-创建环境.png`） |
| 3 | 变量页新建变量 username=tester | 变量表出现（`03-创建变量.png`） |
| 4 | 新建「登录回归流程」→ 添加步骤 → 保存 | 进入流程编辑器，草稿保存成功（`04-流程编排.png`） |
| 5 | 元素库新建「登录按钮」（testid=login-submit） | 元素保存成功（`05-元素库.png`） |
| 6 | 进入运行中心 | 页面正常（`06-运行中心.png`） |
| 7 | 项目设置 → 外观 → 深色 | `<html data-theme="dark">` 生效，界面深色（`07-深色模式.png`） |

## 3. 专项验证

| 专项 | 方法 | 结果 |
| --- | --- | --- |
| 毛玻璃不破坏 Select 交互 | 顶栏 `::before` 伪元素 blur 生效时，环境下拉点击/键盘选择 | ✅ 通过 |
| 时序竞态回归 | management-and-run 场景 ×3、templates ×3 | ✅ 全部通过 |
| 移动端导航 | 375px 下汉堡按钮 → 抽屉展开 → 导航 → 自动收起 | ✅ 通过（实测 transform/class/路由断言） |
| 横向溢出 | 4 页面 × 1440/1024/768/375 四档 | ✅ 全部 `scrollW == clientW` |
| 深色残留 | 真实切换深色后扫描 >230 亮色块 | ✅ 清零（图标底纹已深色化） |
| 按钮一致性 | 全站同上下文按钮尺寸扫描 | ✅ 表格行内统一 small，工具栏统一默认 |

## 4. 说明

- 全量 e2e 长跑在本机存在环境性 flake（真实 Worker 用例偶发超时，原始代码同样存在），本轮自测轮次中未出现。
- 登录页（生产模式专属）由 `test:production` 覆盖；dev 模式无登录页。
- 本轮改动均已提交：`9b758d3`（苹果风格深化）、`1b3eb31`（采集窗口按钮）、`fdbfdd5`（移动端导航 + 表格按钮）并推送远端。
