[pytest]
testpaths = tests
pythonpath = .
# If any test hangs (e.g. a portal/TestClient deadlock), dump all thread
# stacks after this many seconds so the suite fails with a diagnosis instead
# of being killed externally with no evidence. See R-W6 in the review report.
faulthandler_timeout = 60
