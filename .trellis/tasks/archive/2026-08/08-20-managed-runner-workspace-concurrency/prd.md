# 托管 Runner 并发与工作区隔离调度（RUN-01）

## 背景

`ManagedRunner` 目前只有一个工作线程、单一 active 项，无法满足多工作区并发执行目标。

## 目标

- 全局并发默认 2，单工作区并发默认 1，均可通过环境变量覆盖。
- 队列按「eligible FIFO」调度：跳过已达工作区上限的项，不饿死后续工作区。
- 取消/恢复相互隔离：取消一个项不关闭其他项的浏览器/上下文。

## 非目标

- 不实现跨进程/多机执行；仍为单进程内多线程。
- 不改变 SQLite 共享连接模型（另见 DATA/RUN 后续项）。
